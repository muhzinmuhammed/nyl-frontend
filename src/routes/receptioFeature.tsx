
import { Outlet } from "react-router";

const ReceptionFeature = () => {
    return (
        <div className="main-wrapper">
            <Outlet />
        </div>
    );
};

export default ReceptionFeature;
